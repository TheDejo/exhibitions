const PAGE_HEAD = {
    keywords: ['Views Art'],
    pageDescription: 'Welcome to Exhibitions Art Technologies',
};

const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME

const constants = {
    APP_NAME,
    PAGE_HEAD,
};

export default constants;